# CAN-IO-PRO firmware channel

`latest.json` identifies the current encrypted production package. It remains
unavailable until CAN-IO-PRO output GPIOs, drivers and hardware fail-safe
behaviour are finalized and validated.

Only `.avfw` packages created by the private Anchorvolt publishing tool belong
in `packages/`. Development and listener firmware must never be published here.
